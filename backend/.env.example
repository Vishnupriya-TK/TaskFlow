# Backend environment variables
# Replace with your MongoDB connection string
MONGODB_URI="your-mongodb-connection-string"
PORT=5000
# Comma-separated list of allowed origins for CORS (frontend & local dev)
# Example: CORS_ORIGINS=https://taskflow-5tsv.onrender.com,http://localhost:5173
CORS_ORIGINS=https://taskflow-5tsv.onrender.com,http://localhost:5173
